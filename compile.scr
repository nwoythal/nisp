#!/bin/bash

if [ -e parsing ]; then
   rm -f parsing #delete file if it exists
fi
if [ -e parsing.c ]; then
    gcc -std=c99 -Wall parsing.c mpc/mpc.c -ledit -lm -g -o parsing 2>&1 | grep -i error #compile
else
    exit 20 #File not found
fi

